# Getting Started

## Install the dependencies

In the project directory, run:

### `npm i`

## Run the application

In the project directory, run:

### `npm start`

## Run the tests

In the project directory, run:

### `npm test`
